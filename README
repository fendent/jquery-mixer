PlaylistCollection jQuery plugin
  - jquery.mixer.js
  - A jQuery/soundManager2 plugin written by Sean Murphy


Requirements:
  - jQuery (http://jquery.com/)
  - soundManager2 (http://www.schillmania.com/projects/soundmanager2/)


Example:

  var sounds = [
    'mp3/coins.mp3',
    'mp3/800hz.mp3',
    'mp3/walking.mp3',
    'mp3/bass.mp3'
  ];

  soundManager.url = 'swf/';
  soundManager.flashVersion = 9;
  soundManager.consoleOnly = true;

  var plc;

  soundManager.onready(function() { 
    plc = soundManager.PlaylistCollection();
    
    // Standard playlist here.
    var fooPL = plc.createPlaylist({ 'name' : 'foo' });
    fooPL.addSongList(sounds);
    plc.playlists.foo.play();

    // Create an automated playlist with the getNextTrack() callback.
    plc.createPlaylist({
      'id'   : 'radio'
      'auto' : true,
      'getNextTrack' : function() {
        var url = $.ajax(...);

        return url;
      }
    });
  });
